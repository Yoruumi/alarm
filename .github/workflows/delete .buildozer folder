name: Delete .buildozer Folder

on:
  push:
    branches:
      - main  # Adjust to your branch name

jobs:
  delete_folder:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Delete .buildozer folder
      run: rm -rf .buildozer
